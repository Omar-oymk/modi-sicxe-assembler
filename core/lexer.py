# this file takes in the input file
# reads each and every line 
# extracts each, removes comment, extracts output, returns label, instruction, operand list
# the parser will then take each take these and create a list of lines that has multiple data
# that will then be filled on each iteration 

fields = []
